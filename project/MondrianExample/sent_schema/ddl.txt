DROP TABLE IF EXISTS place
CREATE TABLE place
         (
           place_id     INT UNSIGNED,
           place_name   VARCHAR(200),
		   PRIMARY KEY (place_id)
         );
CREATE UNIQUE INDEX idx_place_pk ON place
(
    place_id
);

DROP TABLE IF EXISTS sentence
CREATE TABLE sentence
          (
            book_id      INT UNSIGNED,
            sentence_id  INT UNSIGNED,
			book_name    VARCHAR(5000)
            sentence     VARCHAR(5000)
          );
          
DROP TABLE IF EXISTS sentence_and_place
CREATE TABLE sentence_facts
           (
             book_id     INT UNSIGNED,
             sentence_id INT UNSIGNED,
             place_id    INT UNSIGNED,
             frequency   INT UNSIGNED
           );
           
DROP TABLE IF EXISTS place
CREATE TABLE place
         (
           place_id     INT UNSIGNED,
           place_name   VARCHAR(200)
         );
         
LOAD DATA LOCAL INFILE '/Users/Naix/Tmp/unb-dw/project/XMLtoRel/places.txt' INTO TABLE place
LOAD DATA LOCAL INFILE '/Users/Naix/Tmp/unb-dw/project/XMLtoRel/sentences.txt' INTO TABLE sentence
LOAD DATA LOCAL INFILE '/Users/Naix/Tmp/unb-dw/project/XMLtoRel/sentences_and_places.txt' INTO TABLE sentence_and_place


